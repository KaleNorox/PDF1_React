import React from 'react';
import ReactDOM from 'react-dom';
import './style2.css';
import App from './App'; // Importando el componente App desde su archivo correspondiente

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

